package com.ramesh.scp_project.core.indexing

/**
 * Left intentionally empty. Earlier test scaffolding needed a resolver stub,
 * but the production code now depends on small interfaces instead.
 */
object FakeContentResolver
